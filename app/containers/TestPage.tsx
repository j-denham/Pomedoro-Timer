import React from 'react'

import DbTest from '../features/test/DbTest';

interface TestPageProps {

}

const TestPage: React.FC<TestPageProps> = ({}) => {
        return (
            <DbTest />
        );
}

export default TestPage